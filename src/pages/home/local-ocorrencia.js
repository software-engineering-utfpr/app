import React from 'react';
import { View } from 'react-native';

import './styles';

const LocalOcorrencia = props => {
	const { navigate } = props.navigation;

	return <View />;
};

export default LocalOcorrencia;